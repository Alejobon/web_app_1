"""Repository layer — pure persistence and query operations.

Each repository owns collection access and CRUD helpers for one entity.
No business logic, no cross-entity orchestration — that lives in services/.
"""
